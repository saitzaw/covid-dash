# start the flash and dash appliction 
# note: the script don't know your virutalenv 
# add the virtual environment path in config.py 

export FLASK_APP=wsgi.py 
export FLASK_DEBUG=1 
export APP_CONFIG_FILE=config.py

# run the flask server (you can configure this application  with Apache or NGX) 
flask run 
